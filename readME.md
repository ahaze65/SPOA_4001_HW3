Open up the 'app.py' page, make sure that none of the python code is yellow. If its yellow, pip install the libraries using:

 __Local Python path goes HERE__ -m pip install __library name__


Next, run the app.py then in the terminal run: 

__Local Python path goes HERE__ -m streamlit run app.py